<% @users.each do |user| %>
<div>
  <p>
    image:<%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
  </p>
  <p>Title：<%= user.name %></p>
  <%= link_to "Show", user_path(user.id) %>
  </div>

<% end %>